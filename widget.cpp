#include "widget.h"
#include "graphics.hpp"
using namespace genv;


Widget::Widget(int x, int y, int size_x, int size_y)
    : x(x), y(y), size_x(size_x), size_y(size_y) {}

void Widget::draw()
    {

    }

void Widget::handle(event ev){


}
